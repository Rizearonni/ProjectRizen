[package]
name = "data"
version.workspace = true
edition.workspace = true

[dependencies]
common = { path = "../common" }
worldgen = { path = "../worldgen" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
thiserror = "2"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
